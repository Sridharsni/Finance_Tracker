setuptools>=58.0.0  # Ensure setuptools is installed
wheel                # Ensure wheel is available for building
Flask==2.3.2
pandas==1.5.3
matplotlib==3.6.2
scikit-learn==1.0.2  # Downgrade to a more stable version
numpy>=1.23.2        # Ensure compatible version of numpy
